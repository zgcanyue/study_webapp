orm.py

创建数据库连接池

web App里面有很多地方需要访问数据库，这些访问数据库的代码如果分散到各个函数中，不仅复杂而且不利于维护和代码复用，
所以，把常用的select、insert、update、delete数据库操作用函数封装起来，方便使用。

编写一个简单的ORM(对象-关系映射)，防止注入，不用每次都手写，方便

先定义一个User对象，然后把数据库表User和他关联起来

定义所有ORM的映射基类Model，然后通过metaclass将具体的子类映射信息读取出来