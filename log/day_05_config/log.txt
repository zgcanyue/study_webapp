config.py

编写配置文件

一个Web App在运行时都需要读取配置文件，如数据库的用户名、口令等，在不同的环境中运行时，Web App可以通过读取不同
配置文件来获取正确的配置

默认的配置文件应该完全符合本地开发环境，这样无需任何设置，就可以立刻启动服务器

default作为开发环境的标准配置，override作为生产环境的标准配置，这样既方便在本地开发，又可以随时把应用部署到服务器上